# 通用智能合约审计清单

1. 权限控制是否最小化，敏感操作是否有 `onlyOwner` / `AccessControl` 保护。
2. 是否存在重入风险，外部调用前后状态更新顺序是否正确。
3. 是否使用 `tx.origin` 进行鉴权。
4. 是否存在未检查的低级调用，如 `call` / `delegatecall`。
5. 是否对签名、白名单、nonce、deadline 做了重放保护。
6. 是否存在整数边界、精度误差、四舍五入导致的资产偏差。
7. 是否有事件日志覆盖关键状态变更。
8. 是否存在可批量优化的存储读写和 Gas 浪费点。
